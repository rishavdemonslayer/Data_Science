use universitymanagement;
create table studentleave(USN varchar(20),date varchar(20), time varchar(20));
select * from student;
create table teacherleave(empID varchar(20), date varchar(20), time varchar(20));
select * from teacherleave;
use universitymanagement;
create table subject(USN varchar(20), semester varchar(20), );